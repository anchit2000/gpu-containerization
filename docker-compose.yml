version: '3'
services:
  ssh-contasiner:
    build:
      context: .
      dockerfile: Dockerfile
    ports: 
      - 2222:22
    volumes:
      - /home/anchit/Desktop/Study/containerization/docker-volume:/home